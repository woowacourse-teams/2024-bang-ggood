import styled from '@emotion/styled';

const Button = styled.button`
  border: 1px solid red; 
   height: 10px;
   width: 100px;
   color: hotpink;
`;

const Check = () => {
  return <Button />;
};

export default Check;
