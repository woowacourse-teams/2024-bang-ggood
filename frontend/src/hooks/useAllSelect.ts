// import { useEffect } from 'react';

// const useAllSelect = )selectedOptions => {
//   const [isAllSelected, setIsAllSelected] = useState(selectedOptions.length === totalOptionCount);

//   useEffect(() => {}, [selectedOptions]);
// };

// export default useAllSelect;
