import useChecklistStore from "@/store/useChecklistStore"

const useChecklistQuestionUpdate = () => {
  const {setChecklistAllQuestionList, checklistAllQuestionList} = useChecklistStore()


 const updateIsChecked = (questionId: number) => {
  

 }
  return (
    <div></div>
  )
}


export default useChecklistQuestionUpdate